(function() {
const KIT = window.SCENE_KIT, M = window.OOM_KILLER_MODEL;
window.createOomKillerSteps = function(run) {
  const f = M.fmtMi;
  return [
    {title: 'Kubelet đặt oom_score_adj cho container', pipelineStep: 0, focus: ['kubelet', 'runtime'],
      phases: [{title: 'Kubelet → container runtime: truyền cấu hình OOM',
        desc: KIT.desc('Kubelet suy ra QoS từ resources của Pod, rồi tính <code>oom_score_adj</code> cho từng container trước khi runtime tạo process.', 'Pod Burstable này: <code>1000 − 1000 × ' + f(run.requestMi) + '/' + f(run.nodeCapacityMi) + ' = ' + run.burstableAdj + '</code> (clamp 2..999). BestEffort là <code>1000</code>; Guaranteed là <code>-997</code>. PriorityClass thông thường không phải input của phép tính này.'),
        focus: ['kubelet', 'runtime'], ...KIT.beat('kubelet', 'runtime', 'info', {mark: ['info', 'OOM settings received'], dy: 3.0})}]},
    {title: 'Runtime áp container cgroup v2', pipelineStep: 0, focus: ['runtime', 'cgroup'],
      phases: [{title: 'Container runtime → cgroup: apply memory.max',
        desc: KIT.desc('Runtime tạo container process trong Linux cgroup và áp memory limit của Pod thành <code>memory.max=' + f(run.limitMi) + '</code>.', 'Kịch bản này scope rõ tới <b>cgroup v2</b>. Cờ kubelet <code>singleProcessOOMKill=true</code> giữ hành vi kill một process; nếu dùng group OOM, nhiều process killable trong <i>cùng container cgroup</i> có thể cùng bị kill.'),
        focus: ['runtime', 'cgroup'], ...KIT.beat('runtime', 'cgroup', 'info', {mark: ['info', 'memory.max applied'], dy: 2.8})}]},
    {title: 'Container limit OOM — memory cgroup', pipelineStep: 1, focus: ['burstable-process', 'cgroup'],
      phases: [
        {title: 'App process → memory cgroup: charge allocation', desc: KIT.desc('App process xin thêm <b>' + f(run.allocationMi) + '</b>. Linux memory controller charge allocation vào cgroup của container.', f(run.usedMi) + ' + ' + f(run.allocationMi) + ' = <b>' + f(run.projectedMi) + '</b>, vượt <code>memory.max</code> / limit <b>' + f(run.limitMi) + '</b> đúng <b>' + f(run.excessMi) + '</b>. Đây là <b>memcg OOM</b>, không phải kubelet eviction.'), focus: ['burstable-process', 'cgroup'], scoreMode: true, scoreTitle: 'container cgroup limit', scores: [KIT.gauge('used', run.usedMi, run.limitMi, 'Mi', {tone: 'warn'}), KIT.gauge('requested', run.projectedMi, run.limitMi, 'Mi', {tone: 'danger'}), KIT.gauge('memory.max', run.limitMi, run.limitMi, 'Mi')], ...KIT.beat('burstable-process', 'cgroup', 'danger', {mark: ['danger', 'limit exceeded'], dy: 2.8})},
        {title: 'Memory cgroup → kernel OOM killer: cannot satisfy charge', desc: KIT.desc('Memory cgroup không thể thỏa charge ở giới hạn đã cấu hình, nên kernel đi vào OOM handling cho <b>cgroup đó</b>.', 'Vùng chọn victim của memcg OOM là cgroup bị vượt limit; đừng đọc nó như một cuộc thi BestEffort/Burstable/Guaranteed trên toàn Node.'), focus: ['cgroup', 'oom'], ...KIT.beat('cgroup', 'oom', 'danger', {mark: ['danger', 'memcg OOM'], dy: 3.0})},
        {title: 'Kernel OOM killer → app process: SIGKILL victim', desc: KIT.desc('Kernel chọn và gửi SIGKILL tới một <b>Linux process</b> trong cgroup bị OOM. Pod object không phải đối tượng kernel kill.', 'Container sau đó kết thúc vì process chính đã chết; Kubernetes sẽ quan sát hậu quả ở các bước sau.'), focus: ['oom', 'burstable-process'], ...KIT.beat('oom', 'burstable-process', 'danger', {mark: ['danger', 'SIGKILL · OOM victim'], dy: 2.4})},
        {title: 'Kernel OOM killer → OOM reaper: reclaim address space', desc: KIT.desc('Sau khi đã chọn victim, kernel đánh thức <b>OOM reaper</b> để giải phóng address space của process đó sớm hơn.', 'OOM reaper là Linux kernel helper; nó không phải controller Kubernetes và cũng không chọn Pod thay kubelet.'), focus: ['oom', 'reaper'], ...KIT.beat('oom', 'reaper', 'warn', {mark: ['warn', 'reclaiming victim memory'], dy: 2.8})}
      ]},
    {title: 'Nhánh thay thế — node/global OOM', pipelineStep: 2, focus: ['allocator', 'oom'],
      phases: [
        {title: 'Kernel allocator → OOM killer: separate global-OOM incident', desc: KIT.desc('Đây là <b>nhánh thay thế</b>, không phải bước kế tiếp sau memcg OOM ở trên: toàn Node chỉ còn <b>' + f(run.globalAvailableMi) + '</b>, nhưng allocation cần <b>' + f(run.globalRequestMi) + '</b> sau khi reclaim không đủ.', 'Global OOM không cần container limit bị vượt. Kernel chọn trong phạm vi node bằng badness thực tế + <code>oom_score_adj</code>.'), focus: ['allocator', 'oom'], scoreMode: true, scoreTitle: 'global allocation pressure · alternative', scores: [KIT.gauge('available', run.globalAvailableMi, run.nodeCapacityMi, 'Mi', {tone: 'danger'}), KIT.gauge('allocation', run.globalRequestMi, run.nodeCapacityMi, 'Mi', {tone: 'warn'})], ...KIT.beat('allocator', 'oom', 'danger', {mark: ['danger', 'global OOM'], dy: 3.0})},
        {title: 'Kernel OOM killer → BestEffort process: higher badness wins', desc: KIT.desc('Trong ví dụ global OOM này, kernel chọn <b>worker process</b> của BestEffort Pod. <code>oom_score_adj=1000</code> khiến nó kém được bảo vệ hơn Burstable <code>' + run.burstableAdj + '</code> và Guaranteed <code>-997</code>.', 'Đây là preference, không phải lời hứa tuyệt đối: badness còn phụ thuộc memory usage và kernel state. PriorityClass không phải global-OOM ranking rule.'), focus: ['oom', 'best-effort-process', 'guaranteed-process'], ...KIT.beat('oom', 'best-effort-process', 'danger', {mark: ['danger', 'SIGKILL · global victim'], dy: 2.4})}
      ]},
    {title: 'Trở lại incident limit OOM — kubelet công bố hậu quả', pipelineStep: 3, focus: ['runtime', 'kubelet', 'apiserver'],
      phases: [
        {title: 'Kubelet → container runtime: query container status', desc: KIT.desc('Trở lại incident <b>container limit OOM</b>: kubelet gọi CRI để đọc ContainerStatus. Kubelet là phía khởi tạo request; runtime không tự gọi ngược kubelet.', 'Đây là ranh giới giữa Linux process lifecycle và Kubernetes reconciliation.'), focus: ['kubelet', 'runtime'], ...KIT.beat('kubelet', 'runtime', 'info', {mark: ['info', 'ContainerStatus requested'], dy: 3.0})},
        {title: 'Container runtime → kubelet: return terminated status', desc: KIT.desc('Runtime trả status của process đã exit cho kubelet qua CRI.', 'Đây là response cho status query của kubelet; kernel đã ra quyết định kill từ trước.'), focus: ['runtime', 'kubelet'], ...KIT.beat('runtime', 'kubelet', 'warn', {mark: ['warn', 'container terminated'], dy: 3.0})},
        {title: 'Kubelet → API Server: persist OOMKilled status', desc: KIT.desc('Kubelet ghi <code>containerStatuses[*].state.terminated.reason: OOMKilled</code> lên Pod status qua API Server.', '<code>OOMKilled</code> nói kernel killed a container process. Nó khác <code>Evicted</code>, là một quyết định chủ động của kubelet khi node pressure.'), focus: ['kubelet', 'apiserver'], ...KIT.beat('kubelet', 'apiserver', 'danger', {mark: ['danger', 'OOMKilled recorded'], dy: 3.0})}
      ]},
    {title: 'restartPolicy: Always khởi chạy lại container', pipelineStep: 4, focus: ['kubelet', 'runtime', 'burstable-process'],
      phases: [
        {title: 'Kubelet → runtime: request restart', desc: KIT.desc('Với <code>restartPolicy: Always</code>, kubelet reconcile Pod rồi yêu cầu runtime khởi chạy lại container.', 'Không có ReplicaSet hay scheduler nào tham gia việc restart container trong cùng Pod.'), focus: ['kubelet', 'runtime'], ...KIT.beat('kubelet', 'runtime', 'ok', {mark: ['ok', 'restart requested'], dy: 3.0})},
        {title: 'Runtime → app process: start replacement process', desc: KIT.desc('Runtime tạo process mới cho <b>cùng container</b>. 3D component process quay về Running để nhấn mạnh đây không phải Pod object mới.', 'Nếu memory limit vẫn quá thấp, chuỗi OOM → restart có thể lặp lại và back-off dần thành CrashLoopBackOff.'), focus: ['runtime', 'burstable-process'], ...KIT.beat('runtime', 'burstable-process', 'ok', {mark: ['live', 'restarted'], dy: 2.4})}
      ]}
  ];
};
})();
